# TUI-запуск станка + авто-фоновка Bash-вызовов: волна 4 и эмпирика

Дата: 2026-08-27. Статус: зафиксировано, все тезисы проверены эмпирически.

## 1. Зачем это

Оператор прислал транскрипт ошибок `launch.sh` из TUI и фидбек «старших братьев» по
поводу auto-backgrounding долгих Bash-команд в Claude Code. Два источника расходились
в 8 раз: community-issues (~120s/2min) против реверс-инжиниринга `cli.js`
(`trz=15000`/`oKY=15000` → 15s). Цель — закрыть расхождение эмпирикой, а не догадками,
и зафиксировать поведение для супервайзера.

## 2. Волна 4: три ошибки транскрипта и фиксы (уже в коммитах)

| Ошибка | Причина | Фикс | Коммит |
|---|---|---|---|
| `exit 127` (`./stanok/launch.sh` нет) | cwd TUI ≠ корень проекта | cwd-инвариант: вызовы `launch.sh` из корня проекта; зафиксировано в CLAUDE.supervisor.md §3 | parent `c473943` |
| `exit 2 invalid choice: 'tickets/...'` | `run`-подкоманда потеряла старый протокол `launch.sh <ticket> <label>` | неявный `run` в `main()` (`argv[0] not in (run/status/stop/watch)` → `["run"]+argv`) | sub `b502f96` |
| `exit 13 ticket not found: stanok/stanok/tickets/...` | путь тикета удваивался (REPO_ROOT=stanok/, тикет в корне проекта) | `_resolve_ticket()` — три базы по порядку (корень проекта → корень станка → как передан) + печать списка попыток при rc=13 | sub `b502f96` |

Верификация волны 4: `py_compile` OK; `doctor` 13 ok / 0 fail; smoke implicit-run
rc=15 (label-guard без `run`) и rc=13 (список путей); юнит `_resolve_ticket` по
5 сценариям (проект/станок/cwd-relative/абсолютный/нет). darkcast синхронизирован
(`df2c463`, только `launcher/stanok.py` + `README.md`).

### 2.1 Волна 5: детерминированный cwd + fail-fast --background

Три улучшения после исследования авто-фоновки 2.1.88:

1. **launch.sh:** `cd "$DIR/.."` перед exec → детерминированный cwd = корень проекта.
   Вызов работает из любого места (проверено: `CWD_AFTER_SHIM=~//skeleton`
   при запуске из /tmp), класс багов «запустили не из корня» закрыт на уровне shim.
2. **stanok.py:** fail-fast проверка тикета ДО ветки `--background` → несуществующий
   тикет = rc=13 мгновенно (раньше родитель возвращал 0 и рожал мёртвый детaч,
   умиравший в rc=13 внутри launch.log). Проверено: rc=13 с полным списком путей.
3. **stanok.py:** `os.path.abspath(__file__)` в spawn фонового детaча → надёжность
   re-exec при вызове через относительный путь.

Верификация волны 5: `py_compile`, `bash -n`, `doctor` 13 ok / 0 fail, smoke из /tmp
(rc=13 + CWD-проверка). Коммит: sub (см. git log), darkcast синхронизирован.

### 2.2 Волна 6: dirty-tree гейт (станок отказывается затирать незакоммиченное)

Оператор запустил станок в `~/darkcast/stanok` без коммита — `git reset --hard` +
`git clean -fdq` в `reset_repo()` стёрли незакоммиченные `docs/utils.md` и
`tests/utils.test.js`. Факт подтверждён по `smoke-010.launch.log` (`--- reset репо до
чистого состояния ---` в `cwd=~//darkcast/stanok`) и git status.

Фикс — fail-closed гейт `dirty_tree_gate()` в `cmd_dispatch_run`:

1. Проверяется ДО `reset_repo()` и ДО ветки `--background` → покрывает и родителя,
   и детaч-ребёнка; несуществующего тикета (`rc=13`) не рожает мёртвый детaч.
2. `git status --porcelain` непустой → отказ с полным списком незакоммиченного
   (до 20 строк) и rc=22. Не-git репо не блокируем (это ловит rc=12).
3. Exit-код 22 свободен (заняты 12, 13, 14, 15, 20, 21, 24).
4. Смысл для TUI: перед запуском закоммитить (`git -C stanok add -A &&
   git -C stanok commit --no-verify -m "..."`) — только потом запускать станок.

Верификация волны 6: `py_compile` OK; smoke с грязным деревом → `bg rc=22 (expect 22)`
и `non-bg rc=22 (expect 22)`; на чистом дереве гейт возвращает False. См. rc-таблицу
в `launch.sh` (22=dirty-tree) и `CLAUDE.supervisor.md` §2.

## 3. Эмпирическая модель авто-фоновки Bash-вызова (Claude Code 2.1.88)

**Правило (подтверждено тестами):** Bash-вызов фонуется, когда команда НЕ умещается
в tool-timeout (дефолт 120с). `trz=15000` — это бюджет ПРОВЕРКИ (подождать до ~15с,
затем решить по остатку timeout), а НЕ авто-порог. Отсюда и «расхождение» источников:
15s и 120s — два разных механизма, которые складываются.

### 3.1 Прогоны (голые команды, без `bash -c`/`echo`)

| Команда (весь Bash-вызов) | tool-timeout | Результат |
|---|---|---|
| `sleep 10` | 120с (дефолт) | блок 10с → «completed» |
| `sleep 60` | 120с (дефолт) | блок 60с → «completed» |
| `sleep 90` | 120с (дефолт) | блок 90с → «completed» |
| `sleep 60` | 17с | **фон на ~15с**, уведомление, exit 0 |
| `sleep 60` | 45с | **фон на ~15с**, уведомление, exit 0 |

Признак фона в результате вызова: `Command running in background with ID: … Output
is being written to: …`. Завершение приходит отдельным уведомлением (exit code).

### 3.2 deny-list `_5Y=["sleep"]` — моё раннее чтение НЕ подтвердилось

Голый `sleep 60` как ВСЯ команда фонуется (строки 4–5 таблицы). deny-list либо не
применяется на этом пути, либо матчит не то, что я предполагал. Практически
«sleep блокирует TUI» происходит из-за большого tool-timeout, а не deny-list.

### 3.3 Почему `sleep 10` — НЕ дискриминирующий тест

`sleep 10` завершается на 10с, что меньше ЛЮБОГО порога (15с и 120с) — при любом
пороге результат одинаковый («completed»). Для различения нужна команда в интервале
(15с, 120с): `sleep 60`/`sleep 90`.

## 4. Выживание после рестарта TUI (механизм `--background`)

Проверено на POSIX-уровне. Механизм `--background` — `subprocess.Popen(..., start_new_session=True)`
в `cmd_dispatch_run`, что эквивалентно `setsid()`. Воспроизведение:

```bash
$ setsid -f sh -c 'echo $$ > /tmp/stanok-setsid-test.pid; exec sleep 45' >/dev/null 2>&1; echo $?
0
$ ps -o pid,ppid,sid,pgid,stat,cmd -p $(cat /tmp/stanok-setsid-test.pid)
    PID    PPID     SID    PGID STAT CMD
  76288       1   76288   76288 Ss   sleep 45
# из НОВОГО шелла (аналог переоткрытого TUI), после смерти родителя:
$ ps -p 76288   # ALIVE: PPID=1, SID=PGID=pid, Ss (своя сессия, нет управляющего терминала)
```

Вывод: setsid-детaч переживает смерть родителя (PPID → 1, новая сессия/группа, нет
управляющего терминала → нет SIGHUP; другой pgid → не попадает в group-kill).
Для супервайзера: «закрыл TUI → прогон продолжился → `status`/`stop` работают».

## 5. Практические следствия для супервайзера

1. `launch.sh <ticket> <label> --background` возвращается мгновенно (суб-секунда) —
   **никогда не фонится**, TUI не блокируется. Канонический режим (§3).
2. НЕ-background прогон (реальный 20–40 мин) фонуется при любом пороге — TUI не
   блокируется, но результат придёт уведомлением.
3. Долгий Bash-вызов в TUI с дефолтным 120с таймаутом БЛОКИРУЕТ до ~120с. Если нужно
   фоновое выполнение без блокировки — явный `run_in_background` или малый tool-timeout.
4. Полное отключение авто-фона: `CLAUDE_CODE_DISABLE_BACKGROUND_TASKS`.
5. Порог 40 мин в §3 (зависший прогон) НЕ связан с авто-фоновкой — это про
   `elapsed_s` реального прогона; остаётся в силе.

## 6. Воспроизводимость и артефакты

- Все команды раздела 3 выполнимы как есть (голый `sleep` — весь Bash-вызов).
- Логи фоновых задач: `/tmp/claude-1000/<user>/<session>/tasks/<ID>.output`.
- Фоновые тестовые задачи (`br40dp26y`, `bzaizefr8`) завершились exit 0, процессы
  очищены; файлы /tmp удалены.
- Память пет-проекта обновлена 2026-08-27: «15с-порог» и «sleep не авто-фонится»
  исправлены на эмпирические факты.
