ext = "jq"
test_glob = "*.json"
name_regex = "[A-Za-z0-9_-]+\\.json"
test_runner = "jq empty"
smoke_runner = "jq empty"
preflight = "command -v jq"
# CC-151: `jq empty` has no config file that rewrites its verdict.
verdict_config = []
