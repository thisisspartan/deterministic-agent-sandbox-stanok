# Machine launcher dependencies (Stanok 2.0)
# claude-agent-sdk — Claude Agent SDK (runs claude-code via the API).
# python-telegram-bot — optional Telegram gate (C4); tolerant of missing credentials.
claude-agent-sdk==0.2.139
python-telegram-bot==22.8
