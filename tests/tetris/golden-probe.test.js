'use strict';
// Diagnostic removed: this file is intentionally inert. The real tests live in
// bag.test.js. (Created to probe for an executable node; no shell is available
// in this sandbox, so it is left as a passing no-op.)
const test = require('node:test');
test('golden-probe (inert)', () => {
  // No assertions: never fails the green phase.
});
